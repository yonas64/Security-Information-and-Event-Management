# Feature Y

This package contains ecommerce feature demonstrations that use shared UI components and utility functions.

## Features

- `ProductListFeature` — displays a product catalog and adds products to cart.
- `CartFeature` — displays a cart summary, removes products, and calculates totals.
